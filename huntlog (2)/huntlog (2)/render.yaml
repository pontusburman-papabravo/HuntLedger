services:
  - type: web
    runtime: node
    name: app
    buildCommand: npm install && node bootstrap.mjs
    startCommand: sh _start.sh
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
